# 2026-04-08 工作记录

## 项目：ai-poster AI 海报生成系统

## 今日完成的功能修改

### 1. 自定义字体功能
- 新增 `Font` 数据库模型（name, fileName, url, format）
- 新增 `/api/fonts` API（GET 列表、POST 上传）
- 新增 `useFonts.ts` Hook 自动加载所有字体到页面
- `PosterBuilder.tsx` 支持字体选择下拉框
- 字体文件存储在 `public/fonts/` 目录

### 2. 移除 maxLines 限制，改用按文本高度计算
- 从 `prisma/schema.prisma` 移除 `maxLines` 字段
- 移除所有相关的类型定义、UI 控件、警告提示
- 文本渲染改为根据 `height` 和 `lineHeight` 自动计算可显示行数
- `overflow: hidden` 确保超出部分不显示

### 3. 新增文本对齐功能
- `textAlign`: left / center / right（水平对齐）
- `verticalAlign`: top / center / bottom（垂直对齐）
- 同步更新数据库 schema、类型定义、API 路由、CSS 渲染、Canvas 渲染

### 4. Canvas 文本渲染修复
- 修复预览图与保存图文本不一致问题
- 使用 `textBaseline = "alphabetic"` 匹配 CSS 基线
- y 坐标添加 `fontSize` 偏移量
- 换行算法优先在空格处断行，更接近 CSS 行为

### 5. 生成页响应式布局优化
- 小屏幕纵向布局（flex-col），大屏幕横向布局（flex-row md:flex-row）
- 预览区宽度根据视口高度和画布比例自动计算
- 预览区小屏幕全宽，大屏幕固定宽度并居中

### 6. 编辑器其他优化
- `TemplateEditorClientWrapper.tsx`: 修复 Next.js 16 SSR 问题
- 属性面板添加 max-height 和 overflow-y-auto

## 技术细节

### 数据库变更
```prisma
model TemplateElement {
  // 移除: maxLines Int?
  // 新增:
  fontFamily    String?
  textAlign     String?
  verticalAlign String?
}

model Font {
  id        String @id @default(cuid())
  name      String @unique
  fileName  String
  url       String
  format    String  // ttf / woff / woff2 / otf
}
```

### 文本对齐渲染逻辑
```jsx
// CSS 渲染：使用 flex 容器
<div style={{
  display: "flex",
  flexDirection: "column",
  justifyContent: verticalAlign === "center" ? "center"
                : verticalAlign === "bottom" ? "flex-end"
                : "flex-start",
  textAlign,
}}>

// Canvas 渲染：计算偏移量
const totalHeight = lines.length * lineHeight;
let offsetY = verticalAlign === "bottom" ? el.height - totalHeight
           : verticalAlign === "center" ? (el.height - totalHeight) / 2
           : 0;
```

### Canvas 换行算法
```typescript
// 优先在空格处断行，更接近 CSS word-wrap 行为
for (const char of chars) {
  const testLine = currentLine + char;
  const metrics = ctx.measureText(testLine);

  if (metrics.width > maxWidth && currentLine) {
    const lastSpace = currentLine.lastIndexOf(" ");
    if (lastSpace > 0) {
      lines.push(currentLine.slice(0, lastSpace));
      currentLine = currentLine.slice(lastSpace + 1) + char;
    } else {
      lines.push(currentLine);
      currentLine = char;
    }
  }
}
```

### 响应式布局关键代码
```jsx
<div className="flex flex-col md:flex-row gap-6">
  {/* 预览区 */}
  <div className="md:shrink-0" style={{ width: `min(100%, ${previewWidth}px)` }}>
  {/* 输入区 */}
  <div className="flex-1 md:min-w-0">
```
